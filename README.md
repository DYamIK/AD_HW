## 项目记录：基于 CARLA 0.9.16 的自动驾驶换道决策与轨迹规划作业

> 路径：`/data1/daiyukun/小学一下/AutoDriving/AD_HW/carla_lane_change_notes.md`  
> 作用：后续在本项目中继续实现、调试和写报告时，都优先参考本记录。

---

### 1. 作业核心要求总结

- **场景**：三车道高速公路。
- **车辆数量**：6 辆车（1 辆自动驾驶车 AV + 5 辆人类驾驶车 HV）。
- **目标**：
  - 设计 **换道决策算法**（什么时候、往哪条车道换）。
  - 设计对应的 **运动/轨迹规划算法**（怎么安全平滑地换过去）。
  - 必须在仿真环境中跑通，给出可视化结果和分析。
- **约束与建议**：
  - HV 车辆全部使用 **IDM（Intelligent Driver Model）** 控制，体现不同驾驶风格。
  - 编程语言和平台不限制，可用 Python/C++/Matlab 等。
  - 建议的决策算法：强化学习 / 博弈论 / 有限状态机（FSM）。
  - 建议的轨迹规划：Lattice / 贝塞尔曲线 / 人工势场等。
- **挑战任务（加分）**：
  - **二次换道**：AV 换道后若前车过慢，可以再次换回原车道。

---

### 2. 本项目选型与总体方案

- **仿真平台**：CARLA 0.9.16（已存在目录：`/data1/daiyukun/小学一下/carla-0-9-16-linux`）。
- **编程语言**：Python（推荐 Python 3.8+）。
- **仿真模式**：
  - 使用 **同步模式（synchronous mode）** + 固定时间步长 `dt`（例如 0.05 s）。
  - 方便和 IDM、轨迹规划中的离散时间模型对应。
- **控制架构**：
  - **5 辆 HV**：使用 IDM 做纵向控制（加速度/速度/位置更新），车道固定不变，只做跟车。
  - **1 辆 AV**：上层是 **换道决策（FSM）**，中层是 **轨迹规划（五次多项式 / 贝塞尔横向变道）**，底层把轨迹转成车辆控制命令。

---

### 3. 场景与初始条件（CARLA 中的建模方式）

#### 3.1 三车道建模

- 车道宽度：**4.5 m**（作业给定）。
- 简化做法：在 CARLA 世界坐标中约定：
  - 纵向前进方向为 `x` 轴。
  - 横向车道方向为 `y` 轴。
  - 三条车道中心：
    - 右车道：`y = -4.5`
    - 中车道：`y = 0`
    - 左车道：`y = +4.5`
- CARLA 实现方式：
  - 可选一张较直的地图（如 `Town03` / `Town04`）的一段直路。
  - 或者直接在一片空地上用「虚拟车道」方式：只要保证 `x` 增加表示沿路前进，`y` 取上述三个值即可。

#### 3.2 车辆初始状态（严格对照作业附录 B）

以 AV 初始位置为参考（`x_AV = 0`）：

- **AV（自动驾驶车）**
  - 车道：中间车道（`y = 0`，车道索引记作 `lane = 1`）。
  - 位置：`x_AV = 0`。
  - 初始速度：`v_AV = 80 km/h ≈ 22.22 m/s`。

- **HV1（同车道前车）**
  - 车道：中间车道（`y = 0`）。
  - 位置：`x_HV1 = x_AV + 50 = 50 m`。
  - 速度：`v_HV1 = 70 km/h ≈ 19.44 m/s`。

- **HV2 & HV3（左侧车道车辆）**
  - HV2：左侧车道，AV 后方 20 m。
    - `x_HV2 = x_AV - 20 = -20 m`，`y = +4.5`。
    - `v_HV2 = 75 km/h ≈ 20.83 m/s`。
  - HV3：左侧车道，AV 前方 80 m。
    - `x_HV3 = x_AV + 80 = 80 m`，`y = +4.5`。
    - `v_HV3 = 85 km/h ≈ 23.61 m/s`。

- **HV4 & HV5（右侧车道车辆）**
  - HV4：右侧车道，AV 前方 10 m。
    - `x_HV4 = x_AV + 10 = 10 m`，`y = -4.5`。
    - `v_HV4 = 90 km/h ≈ 25.00 m/s`。
  - HV5：右侧车道，AV 后方 30 m。
    - `x_HV5 = x_AV - 30 = -30 m`，`y = -4.5`。
    - `v_HV5 = 75 km/h ≈ 20.83 m/s`。

实现注意点：

- CARLA 里用统一的 `yaw = 0`、中等高度 `z ≈ 0.3 m` 来 `spawn_actor`。
- 可以为所有车辆选同一车型（如 `vehicle.tesla.model3`），减少干扰因素。

---

### 4. HV 车辆：IDM 模型实现方案

#### 4.1 IDM 基本公式（作业给定）

- 加速度：

\[
a = a_{\max}\left(1 - \left(\frac{v}{v_{\text{desired}}}\right)^4 - \left(\frac{s^*(v, \Delta v)}{s}\right)^2\right)
\]

- 期望间距：

\[
s^*(v,\Delta v) = s_0 + vT + \frac{v \Delta v}{2\sqrt{a_{\max} b}}
\]

其中：

- \(a_{\max}\)：最大加速度；
- \(v\)：当前车速；
- \(v_{\text{desired}}\)：期望速度，这里统一取 \(120 km/h ≈ 33.33 m/s\)；
- \(s\)：与前车实际间距；
- \(\Delta v = v - v_{\text{front}}\)：与前车速度差；
- \(s_0\)：静止最小间隔；
- \(T\)：期望车头时距；
- \(b\)：舒适减速度。

#### 4.2 三种驾驶风格参数

- **保守型 Conservative**
  - \(a_{\max} = 2.0 m/s^2\)
  - \(b       = 2.0 m/s^2\)
  - \(s_0     = 5 m\)
  - \(T       = 2.0 s\)
- **正常型 Normal**
  - \(a_{\max} = 4.0 m/s^2\)
  - \(b       = 2.5 m/s^2\)
  - \(s_0     = 3 m\)
  - \(T       = 1.5 s\)
- **激进型 Aggressive**
  - \(a_{\max} = 6.0 m/s^2\)
  - \(b       = 3.0 m/s^2\)
  - \(s_0     = 2 m\)
  - \(T       = 1.0 s\)

要求：5 辆 HV 至少覆盖三种风格（可自由分配哪辆用哪种）。

#### 4.3 HV 更新步骤（每个仿真步）

1. 对于每辆 HV，确定其所在车道（左/中/右）。
2. 在**同一车道上**找出最近前车（可能是 AV 或其他 HV）：
   - 根据 `x` 坐标大小排序；
   - 前车是所有 `x_front > x_self` 中距离最小的那一辆。
3. 计算：
   - 间距 \(s = x_{\text{front}} - x_{\text{self}} - L_{\text{veh}}\)（`L_veh` 为车辆长度，取 4.5 m 左右，确保 `s ≥ 0`）。
   - 速度差 \(\Delta v = v_{\text{self}} - v_{\text{front}}\)。
4. 用 IDM 公式计算加速度 `a`。
5. 用简单积分更新状态（欧拉离散）：
   - \(v_{\text{new}} = \max(0, v + a \cdot dt)\)
   - \(x_{\text{new}} = x + v \cdot dt + 0.5 a \cdot dt^2\)
6. 将新的 `x_new`、车道 `lane_index` 映射回 CARLA 的 `Location(x_world, y_world, z)` 并更新车辆姿态。

实现建议：

- 用一个 `IDMVehicle` Python 类封装：
  - 属性：当前 `x, lane, v`、风格参数等。
  - 方法：`step(dt, front_vehicle_state)`。
  - 内部更新 CARLA `Actor` 的位置。

---

### 5. AV 车辆：决策与换道策略

#### 5.1 决策总体思路

- 采用 **有限状态机 FSM** 实现换道决策（与作业推荐算法之一对应）。
- 核心任务：
  1. 监控当前车道前车（HV1 或其他）速度和间距，判断是否需要换道。
  2. 在需要换道时，决定换到左车道还是右车道（评估安全性和收益）。
  3. 执行一次换道轨迹。
  4. （可选）完成一次换道后，若新车道前方过慢，则触发**二次换道**回原车道。

#### 5.2 状态设计（建议）

- `KEEP_LANE`：保持当前车道巡航 / 跟车。
- `PREPARE_LC_LEFT`：评估向左换道的可行性（安全 + 效率）。
- `PREPARE_LC_RIGHT`：评估向右换道的可行性。
- `LANE_CHANGE_LEFT`：执行向左的换道轨迹。
- `LANE_CHANGE_RIGHT`：执行向右的换道轨迹。
- （挑战任务）`RETURN_ORIGINAL_LANE`：二次换道回到最初车道。

报告中可以画状态转移图，标出各状态及转移条件。

#### 5.3 一次换道触发条件（示例规则）

在 `KEEP_LANE` 状态下：

1. 找到当前车道的最近前车，记作 `front`。
2. 若满足：
   - 当前与前车距离 `d_front` 小于触发阈值（例如 **40 m**）。
   - 前车速度 `v_front` 明显低于 AV 期望速度（如 `v_front < 90 km/h`）。
3. 则认为当前车道效率较低，需要考虑换道：
   - 计算左右车道（若存在）的**评价函数** \(J_{\text{lane}}\)：

\[
J_{\text{lane}} = w_1 \frac{1}{v_{\text{front,lane}}} + w_2 \frac{1}{gap_{\text{front,lane}}} + w_3 \frac{1}{gap_{\text{rear,lane}}}
\]

   - 在满足安全条件前提下选择 `J` 最小的车道为目标车道。

安全条件（示例）：

- 对目标车道：
  - 前车间距 `gap_front > s_{\text{safe,front}}`（例如 15–20 m）。
  - 后车间距 `gap_rear > s_{\text{safe,rear}}`（例如 15–20 m）。

若左车道更优且安全，则转入 `LANE_CHANGE_LEFT`；右侧类似。

#### 5.4 二次换道逻辑（挑战任务）

- 记录 AV 的「原始车道」索引 `origin_lane`。
- 一次换道完成后（进入新车道且轨迹结束），重新进入 `KEEP_LANE`，但此时车道为新车道。
- 周期性（例如每 1 s）重新评估三个车道的评价函数 \(J_{\text{lane}}\)：
  - 当前车道记为 `cur`，原始车道记为 `origin`。
  - 如果 `J_origin + margin < J_cur`（例如 `margin` 取一个小常数），且原始车道安全距离满足约束，则触发：
    - 状态从 `KEEP_LANE` 进入 `RETURN_ORIGINAL_LANE`，执行一次反向换道轨迹。

---

### 6. AV 车辆：轨迹规划方案

#### 6.1 总体考虑

- 作业只要求「合理的轨迹规划方法」，不强制车辆动力学非常精细。
- 实现建议：
  - 将道路近似为直线，采用 Frenet 或「纵向-横向解耦」方法。
  - **纵向**：保持当前速度或做简单 ACC 控制。
  - **横向**：使用 **五次多项式（Quintic Polynomial）** 或 **三次贝塞尔曲线** 生成平滑变道轨迹。

#### 6.2 五次多项式横向轨迹（推荐）

- 目标：在时间区间 \([0, T]\)（例如 \(T = 3\sim4 s\)）内，从当前车道中心 `d_start` 变到目标车道中心 `d_target`，且起终点横向速度、加速度为 0。

轨迹形式：

\[
d(t) = a_0 + a_1 t + a_2 t^2 + a_3 t^3 + a_4 t^4 + a_5 t^5
\]

约束：

- 初始时刻 \(t = 0\)：
  - \(d(0) = d_0\)（当前车道中心）
  - \(\dot d(0) = 0\)
  - \(\ddot d(0) = 0\)
- 结束时刻 \(t = T\)：
  - \(d(T) = d_f\)（目标车道中心）
  - \(\dot d(T) = 0\)
  - \(\ddot d(T) = 0\)

6 个约束 → 6 个系数，可以写成线性方程组求解。求得 \(a_0 \dots a_5\) 后，就得到整个变道过程中的横向偏移。

#### 6.3 轨迹跟踪与 CARLA 控制

两种实现思路（作业层面都可以接受）：

1. **简化 kinematic 方式**（实现更简单）：
   - 直接把 AV 的质心位置 \((x(t), d(t))\) 当作参考，在每个 `dt` 时刻更新 AV 的 `Transform`：
     - `x(t)` 可按期望纵向速度积分（只在前车太近时减速）。
     - `y(t) = d(t)`。
   - 适合快速完成作业并绘制轨迹。

2. **控制命令方式**（更贴近真实）：
   - 在每个仿真步计算当前姿态与参考轨迹之间的误差，使用简单的横向/纵向控制器（如 Pure Pursuit + PID）输出：
     - `steer`（方向盘）、`throttle`（油门）、`brake`（刹车）。
   - 通过 `vehicle.apply_control()` 施加给 CARLA 车辆。

本项目优先采用**第一种简化方法**保证作业按时完成，如有精力再尝试第二种。

---

### 7. 程序结构规划（Python）

建议在 `AD_HW` 目录下建立如下结构（可在后续实现时调整）：

- `main.py`：入口脚本，负责：
  - 连接 CARLA，设置同步模式和 `dt`。
  - 加载地图，生成 AV 和 5 辆 HV，初始化各自类。
  - 主循环：`for step in range(N): world.tick(); update_HV(); update_AV(); log();`。
- `idm.py`：
  - 定义 `IDMVehicle` 类。
  - 封装 IDM 参数（风格）、状态更新、与前车搜索等逻辑。
- `av_agent.py`：
  - 定义 `AVAgent` 类：
    - 成员：FSM 当前状态、原始车道、目标车道、当前轨迹系数、轨迹时间等。
    - 方法：
      - `perceive(hv_list)`：获取左右车道前后车信息。
      - `decide()`：状态机转移 / 选择目标车道。
      - `plan_lane_change()`：生成五次多项式横向轨迹。
      - `step(dt)`：根据当前状态推进 AV 运动。
- `utils.py`（可选）：
  - 坐标转换函数、车道中心计算、可视化辅助函数等。
- `logs/`：
  - 存放仿真过程数据（CSV / npy 等），便于后续画图。
- `plots.ipynb` 或 `plot_results.py`：
  - 根据日志绘制顶视轨迹图、速度/车道/间距随时间曲线，为报告准备图表。

---

### 8. 报告撰写要点（对应作业要求）

1. **仿真环境描述**：
   - 说明使用 CARLA 0.9.16、Python 版本、硬件环境。
   - 描述三车道搭建方式、车辆数量与初始化位置速度（可配表格和示意图）。
2. **决策算法设计**：
   - 介绍 FSM 各状态（KEEP_LANE / PREPARE / LANE_CHANGE / RETURN 等）。
   - 用流程图或状态转移图展示什么时候触发一次换道、二次换道。
   - 说明评价函数 \(J_{\text{lane}}\) 的结构、权重和安全距离约束。
3. **运动规划算法**：
   - 介绍五次多项式轨迹的约束条件、系数求解方式和时间选择。
   - 如使用简化控制方式，要解释为何作业层面足够合理。
4. **仿真结果与分析**：
   - 提供关键图表：
     - 顶视轨迹图（展示 AV 从中间车道到左右车道、再换回的路径）。
     - 速度-时间曲线、车道编号-时间曲线。
     - 与前车间距-时间曲线（检验安全性）。
   - 对比：
     - 不换道 vs 一次换道 vs 二次换道的平均速度/行程时间。
   - 分析可能的风险场景和算法局限性。
5. **代码说明**：
   - 简要说明主程序结构、关键类和函数。
   - 标明依赖库版本（CARLA、numpy、matplotlib 等）。

---

### 9. 下一步实施计划（简要）

- 第一步：在 `main.py` 中打通 CARLA 连接与同步仿真循环，只生成 1 辆 AV，测试移动。
- 第二步：实现 `IDMVehicle` 类，控制 5 辆 HV 按 IDM 在各车道上行驶。
- 第三步：实现 AV 的 FSM 与一次换道轨迹（先实现单次换到左车道的简单版本）。
- 第四步：增加车道评价函数与对左右车道的选择逻辑，完成一般化一次换道决策。
- 第五步（可选加分）：加入二次换道逻辑和相关可视化对比。


